[project]
name = "eveac-ai"
version = "0.1.0"
description = "Headless EVE Autochess self-play farm (CPU kernel MVP)"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=7.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
